# Secrets — NUNCA commitar
.env
.env.*
!.env.example

# Env injetada no browser em runtime (contém anon key + APP_TOKEN)
app/env.js

# Onboarding local
.claude/HANDOFF.md

# Material de origem do planejamento (pode conter dados/contatos)
chat-export-*.json

# OS / editor
.DS_Store
Thumbs.db
*.log
